# Supabase (optional). If set, the app will use Supabase instead of localStorage.
# Get these from your Supabase project: Project settings → API.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# LLM for chat (optional). If set, the chat panel uses this instead of the built-in rule-based answers.
# OpenAI: get an API key from https://platform.openai.com/api-keys
# Other providers (Groq, Together, etc.): use their API key and set VITE_OPENAI_BASE_URL to their chat completions URL.
VITE_OPENAI_API_KEY=
VITE_OPENAI_BASE_URL=https://api.openai.com/v1
VITE_OPENAI_MODEL=gpt-4o-mini
